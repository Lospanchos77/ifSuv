{
  "name": "@ifsuv/etl",
  "version": "0.0.0",
  "private": true,
  "description": "Script ETL one-shot legacy MySQL → MongoDB (squelette Phase 0, code Phase 6).",
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "migrate": "node dist/index.js",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "clean": "rimraf dist .turbo .tsbuildinfo"
  },
  "dependencies": {
    "@ifsuv/shared": "workspace:*"
  },
  "devDependencies": {
    "@ifsuv/config": "workspace:*",
    "tsx": "4.19.2"
  }
}
